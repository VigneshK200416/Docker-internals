#Docker Internals
Learning how containers actually work under the hood.
#started Git too
Some that like a eye opener is brances is git it thought merge means merging the code of dev-1 and dev-2 but currently understood that its just a pointer
